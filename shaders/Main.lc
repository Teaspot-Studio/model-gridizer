import Phong

main :: Output
main = renderFrame $
   renderPhong (Uniform "modelMat")
               (Uniform "viewMat")
               (Uniform "projMat")
               (Uniform "lightPos")
               (fetch_ "objects" (Attribute "position", Attribute "normal"))